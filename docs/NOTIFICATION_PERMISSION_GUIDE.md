# How to Enable Notification Permissions

If you see the error "Notification permission denied. Enable in browser settings.", follow these steps based on your browser:

## 🌐 Chrome / Edge / Opera

1. **Look for the lock or info icon** (🔒 or ℹ️) in the address bar (left side of the URL)
2. **Click the icon** to open site settings
3. **Find "Notifications"** in the permissions list
4. **Change from "Block" to "Allow"**
5. **Refresh the page** (press F5 or Cmd+R / Ctrl+R)

## 🦊 Firefox

1. **Click the lock/info icon** (🔒 or ℹ️) in the address bar
2. **Click "More Information"** button
3. **Go to the "Permissions" tab**
4. **Find "Notifications"** and click the dropdown
5. **Select "Allow"**
6. **Refresh the page**

## 🍎 Safari

1. **Go to Safari menu** → **Settings** (or **Preferences** on Mac)
2. **Click "Websites" tab**
3. **Select "Notifications"** in the left sidebar
4. **Find this website** in the list
5. **Change to "Allow"**
6. **Refresh the page**

## 📱 Mobile Browsers

### iOS Safari
1. Go to **Settings** → **Safari** → **Notifications**
2. Find this website and enable notifications

### Android Chrome
1. Tap the **three dots menu** (⋮) in the address bar
2. Tap **Settings** → **Site settings** → **Notifications**
3. Find this website and enable notifications

## ⚠️ Important Notes

- **HTTPS Required**: Notifications only work on HTTPS sites (or localhost for development)
- **Permission Reset**: If you previously blocked notifications, you'll need to enable them manually in browser settings
- **Browser Support**: Notifications work best in Chrome, Firefox, Edge, and Safari (latest versions)

## 🔄 After Enabling

1. **Refresh the page** completely (hard refresh: Cmd+Shift+R or Ctrl+Shift+R)
2. **Log in again** if needed
3. The app will automatically request permission again
4. Click **"Allow"** when the browser prompts you

---

**Still having issues?** Make sure:
- Your browser is up to date
- You're not in private/incognito mode (some browsers block notifications)
- Your browser's notification settings aren't globally disabled


